@"
# arconjobs-v2

Dev stack: Docker Desktop + Django (backend) + React/Vite (frontend) + Supabase Local.
Follow the step-by-step setup in the ChatGPT conversation.

Branches:
- dev (active development)
- main (production-ready, later)

"@ | Out-File -Encoding UTF8 README.md